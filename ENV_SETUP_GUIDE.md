# Подробная инструкция по настройке файла .env

Это руководство поможет вам получить все необходимые данные для настройки Telegram-бота техподдержки.

---

## 📋 Содержание

1. [TELEGRAM_BOT_TOKEN - Токен бота](#1-telegram_bot_token---токен-бота)
2. [TELEGRAM_CHAT_ID - ID рабочего чата](#2-telegram_chat_id---id-рабочего-чата)
3. [SMTP настройки для email](#3-smtp-настройки-для-email)
4. [EMAIL_TO - Email получателя](#4-email_to---email-получателя)
5. [Готовый пример .env файла](#5-готовый-пример-env-файла)

---

## 1. TELEGRAM_BOT_TOKEN - Токен бота

### Шаг 1: Откройте BotFather в Telegram

1. Откройте Telegram на любом устройстве
2. В поиске найдите **@BotFather** (официальный бот для создания ботов)
3. Нажмите "Запустить" или отправьте команду `/start`

### Шаг 2: Создайте нового бота

1. Отправьте команду `/newbot`
2. BotFather попросит ввести **имя бота** (как он будет отображаться в списке контактов)
   - Пример: `Техподдержка Компании` или `Support Bot`
3. Затем попросит ввести **username бота** (должен заканчиваться на `bot`)
   - Пример: `my_support_bot` или `company_support_bot`
   - Важно: username должен быть уникальным и заканчиваться на `bot`

### Шаг 3: Получите токен

После создания бота BotFather отправит вам сообщение вида:

```
Congratulations! You have just created a new bot. 
Use this token to access the HTTP API:

1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890

Keep your token secure and store it safely, it can be used by anyone to control your bot.
```

**Этот длинный код и есть ваш `TELEGRAM_BOT_TOKEN`**

### Шаг 4: Сохраните токен

Скопируйте токен и сохраните его - он понадобится для файла `.env`

**Важно:** 
- Никому не показывайте токен
- Если токен утек, используйте `/revoke` в BotFather для создания нового

---

## 2. TELEGRAM_CHAT_ID - ID рабочего чата

Это ID группы или канала, куда бот будет отправлять заявки.

### Вариант 1: Получение ID через бота (Рекомендуется)

1. **Создайте группу в Telegram:**
   - Откройте Telegram
   - Создайте новую группу (Меню → Новая группа)
   - Добавьте участников (можно добавить только себя для начала)

2. **Добавьте бота в группу:**
   - В настройках группы нажмите "Добавить участников"
   - Найдите вашего бота по username (например, `@my_support_bot`)
   - Добавьте его в группу

3. **Сделайте бота администратором (опционально, но рекомендуется):**
   - Настройки группы → Администраторы → Добавить администратора
   - Выберите вашего бота

4. **Получите ID группы через API:**
   - Откройте в браузере следующую ссылку (замените `YOUR_BOT_TOKEN` на ваш токен):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
   - Пример:
   ```
   https://api.telegram.org/bot1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890/getUpdates
   ```

5. **Отправьте любое сообщение в группу** (можно просто написать "test")

6. **Обновите страницу в браузере** - вы увидите JSON ответ

7. **Найдите ID в ответе:**
   Ищите строку вида:
   ```json
   "chat":{"id":-1001234567890,"title":"Название группы","type":"supergroup"}
   ```
   Число после `"id":` (например, `-1001234567890`) - это ваш `TELEGRAM_CHAT_ID`

   **Важно:** ID группы обычно отрицательный и начинается с `-100`

### Вариант 2: Использование специального бота

1. Добавьте в группу бота **@userinfobot**
2. Он автоматически покажет ID группы в сообщении

### Вариант 3: Для личных сообщений (не рекомендуется)

Если хотите получать заявки в личные сообщения:
1. Напишите боту любое сообщение
2. Откройте в браузере: `https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates`
3. Найдите `"chat":{"id":123456789,"first_name":"Ваше Имя"`
4. ID будет положительным числом (например, `123456789`)

---

## 3. SMTP настройки для email

Бот будет отправлять заявки на email. Рассмотрим настройку для популярных почтовых сервисов.

### 3.1. Gmail (Google)

#### Шаг 1: Включите двухфакторную аутентификацию

1. Перейдите на https://myaccount.google.com/security
2. В разделе "Вход в аккаунт Google" найдите "Двухэтапная аутентификация"
3. Включите её, если еще не включена

#### Шаг 2: Создайте пароль приложения

1. Перейдите на https://myaccount.google.com/apppasswords
2. В разделе "Пароли приложений" выберите:
   - Приложение: "Почта"
   - Устройство: "Другое (указать название)" → введите "Telegram Bot"
3. Нажмите "Создать"
4. **Скопируйте 16-значный пароль** (он будет показан один раз!)

#### Шаг 3: Настройки для .env

```env
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш_email@gmail.com
SMTP_PASSWORD=xxxx xxxx xxxx xxxx  # 16-значный пароль приложения (без пробелов)
```

**Важно:** Используйте пароль приложения, а не обычный пароль от Gmail!

### 3.2. Яндекс.Почта

#### Настройки для .env

```env
SMTP_SERVER=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=ваш_email@yandex.ru
SMTP_PASSWORD=ваш_пароль_от_почты
```

**Примечание:** Для Яндекс может потребоваться включить "Доступ по паролю для приложений" в настройках безопасности.

### 3.3. Mail.ru

#### Настройки для .env

```env
SMTP_SERVER=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=ваш_email@mail.ru
SMTP_PASSWORD=ваш_пароль_от_почты
```

### 3.4. Outlook/Hotmail

#### Шаг 1: Создайте пароль приложения

1. Перейдите на https://account.microsoft.com/security
2. Включите двухфакторную аутентификацию
3. Создайте пароль приложения

#### Настройки для .env

```env
SMTP_SERVER=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USER=ваш_email@outlook.com
SMTP_PASSWORD=пароль_приложения
```

### 3.5. Другие почтовые сервисы

Для других SMTP серверов найдите настройки в документации вашего почтового провайдера.

**Общие параметры:**
- **SMTP_SERVER** - адрес SMTP сервера (обычно `smtp.домен.com`)
- **SMTP_PORT** - порт (обычно `587` для TLS или `465` для SSL)
- **SMTP_USER** - ваш email адрес
- **SMTP_PASSWORD** - пароль или пароль приложения

---

## 4. EMAIL_TO - Email получателя

Это email адрес, куда будут приходить заявки от пользователей.

### Варианты:

1. **Личный email** - ваш рабочий email
2. **Групповой email** - email группы техподдержки (например, `support@company.com`)
3. **Специальный email** - создайте отдельный email для заявок

**Пример:**
```env
EMAIL_TO=support@company.com
```

или

```env
EMAIL_TO=your.email@gmail.com
```

---

## 5. Готовый пример .env файла

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# ============================================
# Telegram Bot Configuration
# ============================================
# Токен бота от @BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890

# ID группы/чата, куда отправлять заявки (обычно отрицательное число)
TELEGRAM_CHAT_ID=-1001234567890

# ============================================
# Email Configuration (Gmail пример)
# ============================================
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com
SMTP_PASSWORD=xxxx xxxx xxxx xxxx
EMAIL_TO=support@company.com

# ============================================
# Database Configuration
# ============================================
# Оставьте по умолчанию для SQLite
DATABASE_URL=sqlite:///tickets.db

# ============================================
# Files Storage
# ============================================
# Папка для хранения файлов заявок
FILES_DIR=files
```

---

## ✅ Проверка настроек

После создания файла `.env` проверьте:

1. **Все значения заполнены** (нет пустых строк для обязательных параметров)
2. **Токен бота правильный** - можно проверить, отправив `/start` боту
3. **ID чата правильный** - бот должен быть добавлен в группу
4. **SMTP настройки правильные** - можно протестировать отправку email

---

## 🔒 Безопасность

**Важно:**
- ✅ Файл `.env` уже добавлен в `.gitignore` - он не попадет в Git
- ✅ Никогда не публикуйте токены и пароли
- ✅ Если токен утек - создайте новый через BotFather (`/revoke`)
- ✅ Используйте пароли приложений, а не основные пароли

---

## 🆘 Помощь

Если возникли проблемы:

1. **Бот не отвечает:**
   - Проверьте правильность токена
   - Убедитесь, что бот запущен (`python main.py`)

2. **Заявки не приходят в чат:**
   - Проверьте `TELEGRAM_CHAT_ID`
   - Убедитесь, что бот добавлен в группу

3. **Email не отправляется:**
   - Проверьте SMTP настройки
   - Для Gmail используйте пароль приложения
   - Проверьте, что порт правильный (587 для TLS, 465 для SSL)

4. **Ошибки при запуске:**
   - Проверьте, что все значения в `.env` заполнены
   - Убедитесь, что нет лишних пробелов
   - Проверьте логи бота

---

## 📝 Чек-лист

Перед запуском бота убедитесь:

- [ ] Создан бот через @BotFather
- [ ] Получен и сохранен TELEGRAM_BOT_TOKEN
- [ ] Создана группа для заявок
- [ ] Бот добавлен в группу
- [ ] Получен TELEGRAM_CHAT_ID
- [ ] Настроен SMTP (Gmail/Яндекс/другой)
- [ ] Создан пароль приложения (для Gmail)
- [ ] Указан EMAIL_TO
- [ ] Файл `.env` создан и заполнен
- [ ] Все значения проверены

**Готово! Теперь можно запускать бота: `python main.py`**

