# Быстрая инструкция по настройке .env

## 🚀 Быстрый старт (5 минут)

### 1. Токен бота (TELEGRAM_BOT_TOKEN)

1. Откройте Telegram → найдите **@BotFather**
2. Отправьте `/newbot`
3. Введите имя бота (например: "Техподдержка")
4. Введите username (например: `my_support_bot`)
5. **Скопируйте токен** из ответа BotFather

```
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

### 2. ID чата (TELEGRAM_CHAT_ID)

1. Создайте группу в Telegram
2. Добавьте вашего бота в группу
3. Отправьте любое сообщение в группу
4. Откройте в браузере (замените YOUR_BOT_TOKEN):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
5. Найдите `"chat":{"id":-1001234567890`
6. **Скопируйте число после "id":** (обычно отрицательное)

```
TELEGRAM_CHAT_ID=-1001234567890
```

### 3. Email настройки (Gmail)

1. Включите двухфакторную аутентификацию: https://myaccount.google.com/security
2. Создайте пароль приложения: https://myaccount.google.com/apppasswords
   - Приложение: "Почта"
   - Устройство: "Telegram Bot"
3. **Скопируйте 16-значный пароль**

```
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш_email@gmail.com
SMTP_PASSWORD=xxxx xxxx xxxx xxxx
EMAIL_TO=support@company.com
```

### 4. Создайте файл .env

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=-1001234567890
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш_email@gmail.com
SMTP_PASSWORD=пароль_приложения
EMAIL_TO=support@company.com
DATABASE_URL=sqlite:///tickets.db
FILES_DIR=files
```

## 📖 Подробная инструкция

См. файл `ENV_SETUP_GUIDE.md` для детальных инструкций с примерами.

## ✅ Проверка

После создания `.env` запустите:
```bash
python main.py
```

Если есть ошибки - проверьте, что все значения заполнены правильно.

