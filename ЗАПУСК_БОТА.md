# Как запустить бота

## 📍 Где выполнять команды

Команды нужно выполнять в **терминале** (командной строке) Windows.

### Вариант 1: PowerShell (рекомендуется)
1. Нажмите `Win + X` и выберите "Windows PowerShell" или "Терминал"
2. Или нажмите `Win + R`, введите `powershell` и нажмите Enter

### Вариант 2: Через проводник
1. Откройте папку проекта: `C:\Users\shiva\telegram-support-bot`
2. В адресной строке проводника введите `powershell` и нажмите Enter
3. Откроется PowerShell в этой папке

### Вариант 3: В Cursor/VS Code
1. В Cursor нажмите `Ctrl + ~` (тильда) для открытия терминала
2. Или меню: View → Terminal

---

## 🚀 Запуск бота

### Шаг 1: Перейдите в папку проекта
```powershell
cd C:\Users\shiva\telegram-support-bot
```

### Шаг 2: Активируйте виртуальное окружение
```powershell
.\venv\Scripts\Activate.ps1
```

После активации вы увидите `(venv)` в начале строки:
```
(venv) PS C:\Users\shiva\telegram-support-bot>
```

### Шаг 3: Запустите бота
```powershell
python main.py
```

Вы должны увидеть:
```
INFO - База данных инициализирована
INFO - Бот запущен и готов к работе
```

---

## 🛑 Остановка бота

Чтобы остановить бота:
1. Нажмите `Ctrl + C` в терминале, где запущен бот
2. Бот остановится и вернется в командную строку

---

## ✅ Проверка работы

После запуска:
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Бот должен ответить

---

## ⚠️ Если возникают проблемы

### Ошибка "Python was not found"
- Убедитесь, что виртуальное окружение активировано
- Проверьте, что Python установлен

### Ошибка "ModuleNotFoundError"
- Активируйте виртуальное окружение: `.\venv\Scripts\Activate.ps1`
- Установите зависимости: `pip install -r requirements.txt`

### Бот не отвечает
- Проверьте, что бот запущен (должны быть сообщения в терминале)
- Проверьте файл `.env` - все параметры должны быть заполнены

---

## 📝 Быстрая команда для запуска

Если вы уже в папке проекта, выполните:
```powershell
.\venv\Scripts\Activate.ps1
python main.py
```

Или одной строкой:
```powershell
.\venv\Scripts\python.exe main.py
```

