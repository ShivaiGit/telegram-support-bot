# Инструкция по запуску бота

## ✅ Файл .env готов!

Ваш файл `.env` настроен и содержит все необходимые параметры.

## 🚀 Запуск бота

### Шаг 1: Установите Python (если еще не установлен)

1. Скачайте Python 3.8+ с https://www.python.org/downloads/
2. При установке **обязательно отметьте** "Add Python to PATH"
3. Перезапустите терминал после установки

### Шаг 2: Создайте виртуальное окружение

Откройте терминал в папке проекта и выполните:

```powershell
# Создание виртуального окружения
python -m venv venv

# Активация (PowerShell)
venv\Scripts\Activate.ps1

# Если возникает ошибка выполнения скриптов, выполните:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Шаг 3: Установите зависимости

```powershell
pip install -r requirements.txt
```

### Шаг 4: Запустите бота

```powershell
python main.py
```

## ✅ Проверка работы

После запуска вы должны увидеть:
```
INFO - База данных инициализирована
INFO - Бот запущен и готов к работе
```

## 🧪 Тестирование

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте команду `/start`
4. Бот должен ответить приветствием

## 📝 Создание тестовой заявки

1. Отправьте команду `/new`
2. Следуйте инструкциям бота:
   - Введите имя
   - Введите телефон
   - Введите email (или пропустите)
   - Опишите проблему
   - Выберите приоритет
   - Прикрепите файлы (опционально)
3. Подтвердите отправку

## 🔍 Проверка работы

После создания заявки проверьте:

1. **В Telegram группе** (TELEGRAM_CHAT_ID):
   - Должно прийти сообщение с заявкой

2. **На email** (EMAIL_TO):
   - Должно прийти письмо с заявкой

3. **В базе данных**:
   - Файл `tickets.db` должен быть создан
   - Заявка должна быть сохранена

## ⚠️ Возможные проблемы

### Ошибка "Python was not found"
- Установите Python и добавьте в PATH
- Перезапустите терминал

### Ошибка "ModuleNotFoundError"
- Убедитесь, что виртуальное окружение активировано
- Выполните: `pip install -r requirements.txt`

### Ошибка конфигурации
- Проверьте файл `.env` - все значения должны быть заполнены
- Убедитесь, что нет лишних пробелов

### Бот не отвечает
- Проверьте правильность токена
- Убедитесь, что бот запущен
- Проверьте логи на наличие ошибок

### Email не отправляется
- Для Gmail используйте пароль приложения, а не обычный пароль
- Проверьте SMTP настройки
- Убедитесь, что двухфакторная аутентификация включена

## 📊 Логи

Все действия бота логируются в консоль. Следите за сообщениями:
- `INFO` - информационные сообщения
- `ERROR` - ошибки (требуют внимания)
- `WARNING` - предупреждения

## 🛑 Остановка бота

Нажмите `Ctrl+C` в терминале для остановки бота.

---

**Готово! Бот должен работать. Если возникнут проблемы - проверьте логи и настройки.**

